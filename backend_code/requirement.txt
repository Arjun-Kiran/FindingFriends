# Runtime dependencies — what the server needs to serve a game.
# Test-only packages live in requirement-dev.txt so they stay out of the
# production image.
Flask==3.1.3
Flask-SocketIO==5.6.1
gunicorn==26.0.0
pydantic==2.13.4
simple-websocket==1.1.0
